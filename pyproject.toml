[project]
name = "my-project"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "scripts/pandapower-physics",
]
