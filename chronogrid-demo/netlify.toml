[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[env.production]
  environment = { NODE_ENV = "production" }

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
